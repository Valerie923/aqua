# Render blueprint: one web service. Set GEMINI_API_KEY in the dashboard.
services:
  - type: web
    name: streamcheck
    runtime: docker
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false
    disk:
      name: data
      mountPath: /app/data
      sizeGB: 1
