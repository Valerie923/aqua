# Copy to .env (or set in your host's environment) — never commit real keys.
# Gemini (free tier) — get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=AIza...
# Alternative provider (used only if GEMINI_API_KEY is unset)
# ANTHROPIC_API_KEY=sk-ant-...
# Optional overrides
# VISION_MODEL=gemini-2.5-flash
# DATA_DIR=./data
# FHIR_SERVER_URL=https://hapi.fhir.org/baseR4
